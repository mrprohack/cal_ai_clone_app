RewriteEngine On

# If the file or directory exists in public_html, serve it directly
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Otherwise, send all requests to index.php (Next.js Reverse Proxy)
RewriteRule ^(.*)$ index.php [QSA,L]
